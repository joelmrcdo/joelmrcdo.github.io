source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# To use a different theme from RubyGems, add it below and run `bundle update`
# so that Gemfile.lock records the theme version. Example:
#
# gem "minima"
